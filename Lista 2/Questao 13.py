"""
13. Escreva uma funcão reps em Python que recebe uma lista e retorna uma outra lista contendo apenas
os elementos que aparecem duas ou mais vezes na lista de entrada.

"""


def reps(lista):

for indice, valor in enumerate(lista):
    if x == valor:
        posicao.append(indice)



lista = []

for i in range(3):
    lista.append(float(input('Informe os valores da lista: ')))



print(f"\nA quantidade de vezes que o número escolhido aparece são: {vetor.count(x)}\n"
      f"nas posições: {posicao}")


